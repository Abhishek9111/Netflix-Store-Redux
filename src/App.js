
import React from 'react';
import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  View,
  Text,
  StatusBar,
} from 'react-native';

const App = ()=>{
  return(
    <ScrollView>
      <Text>Hi</Text>
    </ScrollView>
  )
}

export default App;
